import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.azurlanefleettechtracker.retropaint',
  appName: 'azur-lane-fleet-tech-tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
